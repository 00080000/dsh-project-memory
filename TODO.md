# 待办 / 已知瓶颈

按优先级排序，做完的条目挪进 CHANGELOG 对应版本。

## 高优先级（正式规模使用前必须处理）

### 增量持久化与异步 IO——同步阻塞随库体积线性放大

**现状**：每次懒索引 = 全量 `load()`（四个 JSON 全解析）+ 处理 + 全量 `save()`（全部重序列化 + 同步写盘）。`readFileSync` / `writeFileSync` / `JSON.parse` / `stringify` 全部阻塞事件循环。watch 每 15 秒对全仓库做一次同步 `walkDir` + 逐文件 `statSync`。

**影响**：约几百条目内无感；几千条目、entries.json 数 MB 后，每读一个文件伴随几十毫秒的全库读写，模型流式输出可感知顿挫，watch 每个轮询周期一次微卡顿。

**方向**：
- 存储分片或脏键增量写：只重写变化的文件分片，避免每次全量 stringify
- load/save 改 `node:fs/promises` 异步版本
- watch 以 `fs.watch` 事件驱动为主、轮询兜底；或拉长间隔 + 错峰扫描
- LLM 摘要阶段考虑释放 store 锁或分批提交，长摘要不再阻塞同库的 `remember`/`index_doc`

**验收标准**：5000 条目规模的库，单次懒索引的主线程阻塞 < 5ms。

### 召回失准时的内省能力

**现状**：`query_memory` 无命中时，用户无法区分"没索引过"和"索引了但没命中"，只能手翻 index.json。

**方向**：
- 最低成本版：query_memory 无命中时输出追加一行库存概况（N 文件 / M 条目 / 最近索引时间）
- 进一步：stats 工具列出已索引文件清单与条目数

**验收标准**：不看 JSON 文件就能回答"我的记忆库里有什么"。

## 中优先级

### 经验笔记相似覆盖阈值偏宽

`findSupersede` 按 token 重叠 ≥0.6 判定同一问题直接覆盖，措辞相近的两个不同问题可能互相吞掉（短问题尤其容易蹭掉长笔记）。remember 的返回值会告知模型发生了覆盖，但覆盖本身不可逆。候选：提高阈值、要求双向重叠、或命中时返回相似条目让模型决定。

### doc↔symbol 链接的 CJK 符号名匹配

拉丁符号名已启用词边界正则；含 CJK 的符号名仍走子串 `includes`，误链接概率高于拉丁名。CJK 没有词边界，需按 token 或整词匹配。

### CJK 分词只出 bigram

`tokenizeRaw` 对 CJK 连续串只产出二元组，不保留整词。查询与索引共用同一分词所以不存在切分分歧，但长词的完整词形永远不在索引里，是精度上限。候选：整词 + bigram 混合。

### pdfjs OPS 改为按需导入

顶层 `import { OPS }` 目前安全（pdfjs-dist 是自锁定的依赖），但 OPS 只在未启用的 `collectLayoutStats` 分支用到；改成该分支内动态 import 可少一个顶层耦合。

### 符号扫描的注释裁剪会裁到字符串内 // 与 #

行首锚定的声明正则使实际漏扫几乎不会发生，但裁剪逻辑本身不精确；若未来加入非行首锚定的扫描模式需先重写此处。

### 惰性孤儿清理

watch 轮询的孤儿清理依赖整轮扫描。可在 query_memory 命中时顺手 stat 源文件，失效条目即时剔除并标记，降低对轮询的依赖。

### tree-sitter 替换正则符号扫描

正则方案有已知误报/漏报（注释符号、宏、多行签名）。tree-sitter 原生模块违背轻量原则——先评估 wasm 构建的可行性与体积代价。（技术文章里埋了伏笔）

### 长尾问句召回优化

BM25 词面匹配对口语化长问句召回偏弱。候选：查询侧同义词扩展（已有 llmQueryExpansion 开关）、经验层 problem 的改写归一。

## 记录在案但明确不做

- 多实例并发写同一项目存储：进程内锁不跨进程，README 已声明规避方式
