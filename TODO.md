# 待办 / 已知瓶颈

按优先级排序，做完的条目挪进 CHANGELOG 对应版本。

## 高优先级（正式规模使用前必须处理）

### 增量持久化与异步 IO——分片与缓存已落地，剩余两项

**已完成**：entries 分片（每源文件自描述 JSON，写入只落脏分片）+ 进程级 store 缓存（同路径单实例，热路径零全量读写）+ 旧布局自动迁移。

**剩余**：
- 锁范围缩小：LLM 摘要移出锁外（四处：lazy / index-doc / watch.pollRoot / index-repo——index-repo 最严重，整仓一把锁串行所有 LLM）；重校验哈希防竞态；index-repo 的删除 sweep 最后短持锁统一做
- 异步 IO：load/save 改 `node:fs/promises`。热路径阻塞已由分片大幅缓解，此项优先级降低；冷启动批量读分片时需限并发（防 EMFILE）

**不做**：fs.watch 事件驱动（跨平台行为不一致，维持轮询为主——与项目详解的论证一致）

**验收标准**：5000 条目规模的库，热路径单次懒索引的主线程阻塞 < 5ms（基准脚本待写，需区分冷启动与热路径）。

## 中优先级

### 经验笔记相似覆盖阈值偏宽

`findSupersede` 按 token 重叠 ≥0.6 判定同一问题直接覆盖，措辞相近的两个不同问题可能互相吞掉（短问题尤其容易蹭掉长笔记）。remember 的返回值会告知模型发生了覆盖，但覆盖本身不可逆。候选：提高阈值、要求双向重叠、或命中时返回相似条目让模型决定。

### doc↔symbol 链接的 CJK 符号名匹配

拉丁符号名已启用词边界正则；含 CJK 的符号名仍走子串 `includes`，误链接概率高于拉丁名。CJK 没有词边界，需按 token 或整词匹配。

### CJK 分词只出 bigram

`tokenizeRaw` 对 CJK 连续串只产出二元组，不保留整词。查询与索引共用同一分词所以不存在切分分歧，但长词的完整词形永远不在索引里，是精度上限。候选：整词 + bigram 混合。

### pdfjs OPS 改为按需导入

顶层 `import { OPS }` 目前安全（pdfjs-dist 是自锁定的依赖），但 OPS 只在未启用的 `collectLayoutStats` 分支用到；改成该分支内动态 import 可少一个顶层耦合。

### 符号扫描的剩余盲区

六个语言家族均已启用字符串/注释掩码（含 Rust 嵌套块注释、Shell `${#}` 边界）。剩余差距：多行签名续行目前仅 JS/TS 支持；宏展开、复杂泛型边界等极端形态仍有漏报。若实际使用中暴露，可按需补。

### 惰性孤儿清理

watch 轮询的孤儿清理依赖整轮扫描。可在 query_memory 命中时顺手 stat 源文件，失效条目即时剔除并标记，降低对轮询的依赖。

### tree-sitter AST 解析（评估后搁置）

轻量扫描器已覆盖声明级提取的主要误差来源（见上）。tree-sitter wasm 体积（每语言 0.1–1 MB）与启动开销相对剩余收益偏高，暂不引入；若未来出现真实用户的符号精度反馈再评估。（技术文章里埋了伏笔）

### 长尾问句召回优化

BM25 词面匹配对口语化长问句召回偏弱。候选：查询侧同义词扩展（已有 llmQueryExpansion 开关）、经验层 problem 的改写归一。

## 记录在案但明确不做

- 多实例并发写同一项目存储：进程内锁不跨进程，README 已声明规避方式
